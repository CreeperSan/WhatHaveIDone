{
  "name": "what_have_i_done",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build": "electron-packager . what_have_i_done --win --out ./build --arch=x64 --app-version=1.0.0 --electron-version=5.0.6 --icon=./icon.ico"
  },
  "author": "CreeperSan",
  "license": "MIT",
  "devDependencies": {
    "electron": "^18.3.7"
  }
}
